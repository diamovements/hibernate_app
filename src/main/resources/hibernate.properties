# configure of data source (database)

hibernate.driver_class=com.mysql.jdbc.Driver
hibernate.connection.url=jdbc:mysql://localhost/db1
hibernate.connection.username=root
hibernate.connection.password=cocohelliu15

# configure of hibernate

# which db is going to be used for hibernate
hibernate.dialect=org.hibernate.dialect.MySQLDialect
# to see which sql query is generated by hibernate in console
hibernate.show_sql=true
# context of session for correct work of hibernate
# hibernate.current_session_context_class=thread